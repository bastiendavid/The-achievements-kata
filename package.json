{
  "name": "achievements-kata",
  "version": "1.0.0",
  "description": "Achievements kata application",
  "scripts": {
    "test": "mocha",
    "build": "rm -rf dist && cp -r public dist && cp node_modules/requirejs/require.js dist/script && tsc",
    "start": "firebase serve",
    "deploy": "npm run build && firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.1",
    "@types/mocha": "^5.2.7",
    "amd-loader": "0.0.8",
    "chai": "^4.2.0",
    "firebase-tools": "^7.1.2",
    "mocha": "^10.2.0",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "dependencies": {
    "requirejs": "^2.3.6"
  }
}
